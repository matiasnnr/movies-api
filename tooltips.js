// Configure your Atlas credentials
// In the app.js file, search for the variable uri and replace the placeholder text with the connection string below.

// const uri = "mongodb+srv://db_user_movies:<password>@cluster0.s28rk.mongodb.net/<dbname>?retryWrites=true&w=majority";

// Replace <password> with the password for the db_user_movies user. 
// Replace <dbname> with the name of the database that connections will use by default. 
// Ensure any option params are URL encoded.